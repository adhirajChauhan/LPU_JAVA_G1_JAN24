import java.util.HashSet;
import java.util.Set;

class LPU{
    public static void main(String args[]){

        // Set<Integer> set1 = new HashSet<>();

        // set1.add(1);
        // set1.add(2);

        // Set<Integer> set2 = new HashSet<>();

        // set2.add(2);
        // set2.add(1);
        // set2.add(3);

        // set1.addAll(set2);

        // System.out.println(set1);


        // HashSet<Integer> set1 = new HashSet(20, 0.5);

        //capacity -> number of elements set1 can store, by default this is 16.
        //load factor -> whenever out set 1 is filled 50%, a new set of double capacity will be created and all the elements will be moved. By default the load factor is 75%.
    }
}
